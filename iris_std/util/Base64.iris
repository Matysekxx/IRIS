import { encode } "native:Base64"
import { decode } "native:Base64"

/// Base64 encoding and decoding (binary-to-text).
class Base64 {
    static fun encode(input: string): string {
        return encode(input)
    }

    static fun decode(input: string): string {
        return decode(input)
    }
}
