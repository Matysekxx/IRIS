// IRIS Standard Library - iris.lang.Process
from System import native time
from System import native exit as nativeExit

class Process {
    static fun exit(code: int) {
        nativeExit(code)
    }

    static fun uptime(): double {
        return time()
    }
}
