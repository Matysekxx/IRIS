/// One element of a sequence. Advance with next() until hasNext() is false.
export interface Iterator<T> {
    fun hasNext(): bool
    fun next(): T
}

/// A type that can produce an Iterator, enabling for-in iteration.
export interface Iterable<T> {
    fun iterator(): Iterator<T>
}
