from System import native time

fun main() {
    val n = 50000
    val start = time()
    var s = ""
    for (var i = 0; i < n; i = i + 1) {
        s = s + "a"
    }
    val end = time()
    print("Test 4: String Concat (50k): " + (end - start) + " ms")
}
main()
